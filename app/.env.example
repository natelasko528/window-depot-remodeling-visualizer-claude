# Server-side only. Never exposed to the browser.
OPENAI_API_KEY=sk-...

# Exact image model id from your OpenAI dashboard.
OPENAI_IMAGE_MODEL=gpt-image-1

# Optional. Rendered size sent to the images API.
OPENAI_IMAGE_SIZE=1536x1024
